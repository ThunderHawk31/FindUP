---
description: Safely stage, review diff, and commit
---
1. Run `git status` and `git diff --staged`
2. Show me the diff summary before committing
3. NEVER force-push or rewrite history
4. Use conventional commit format (feat/fix/chore)
5. Do NOT create commits unless I explicitly confirm
